# Remove the tab action menu

The user explicitly requested removing each tab's three-dot button and its complete pin, move and split menu. Dragging already provides the desired placement interaction; popup dismissal and position repairs are outside this request. Tabs retain their title, close action, drag insertion and docking, double-click pinning, and existing keyboard shortcuts. Group operations and public service types remain available to consumers.

This intent revision advances state to `0.4.0-draft.6`. Candidate 2 binds the preceding tab-gesture state and becomes historical evidence without modification; current candidate selection is cleared until a replacement is sealed. The user requested minimal verification: one owned build and one focused smoke, with obsolete menu assertions updated rather than preserved. Installation and restart are authorized for the coordinating Agent's combined deployment; this implementation slice touches only the isolated sidebar checkout.

The implementation removes menu state, popup rendering, its button and helpers, menu-only styles and eight locale keys in both languages. `DSH_CHECKOUT=/root/dsh-resource-workbench-candidate/harness bash scripts/build.sh` passed, including Node and Client declaration compilation. `DSH_CHECKOUT=/root/dsh-resource-workbench-candidate/harness node node_modules/vitest/vitest.mjs run tests/right-sidebar.client.spec.tsx -t 'keeps pinning, dragging and closing without a tab action menu'` passed one smoke with 29 other cases skipped. It observes absent menu controls, double-click pinning, drag insertion and closing. No full suite or additional behavior test ran.
