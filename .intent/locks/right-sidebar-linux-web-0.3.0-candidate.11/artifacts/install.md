# Candidate 11 local realization

Applicable implementation: sidebar commit `a6e9cdfb14b6a376f619e434745f78d038cf4595`, governed paths in LOCK.json. Applicable Host baseline: DeepSeek Harness alpha.2 `0a53fb55bea101816fa226bb964ae2bed71c343b`. Companion sources used in deployment: viewer `4bc2a097d5b98d20468c5cd777b3896880e474e5`, manager `f2f06cae17bbd8bc28294e4c5f1cf9d032f7e4a4`.

Inspect the Host, profile, markers and receipt first. `patches/deepseek-harness.patch` is the complete owned intervention, not a license to replace dirty Host files. A clean baseline uses `git apply --unidiff-zero --check` before applying; an existing installation must match its recorded predecessor and receive a reviewed delta. Exact reverse-check plus the matching SHA-256 receipt establish the installed contribution. This deployment upgraded only AppFrame and its retained-session expectation from the previous owned patch, preserving all unrelated edits. New UI-layout artifacts were built with `pnpm exec tsc -b packages/client/ui-layout/tsconfig.json` and `DSH_BUILD_FACE=client pnpm --dir packages/client/ui-layout exec tsdown`.

Build the plugin with `DSH_CHECKOUT=<checked-host> bash scripts/build.sh`. Build the viewer and manager through their own `DSH_CHECKOUT=<checked-host> pnpm build` commands, in dependency order. Use a private Home with the intended full package set and a cold Web process before changing the real profile. On this machine the authorized profile transaction was `DSH_HOME=/root/.dsh node --import tsx/esm apps/cli/src/bin.ts plugin --profile web add /root/dsh-right-sidebar /root/dsh-file-viewer/packages/dsh-file-viewer /root/dsh-file-viewer/packages/dsh-file-viewer-editor /root/dsh-file-manager/packages/dsh-file-manager`, from the checked Host. Verify manifest, lockfile, resolved links, Bundle membership, configuration dump and actual browser entries; `plugin list --depth 0 --json` exposes links omitted by this pnpm version's `why` output.

Activation used the already-authorized `systemctl restart dsh-web`. Service activity alone was followed by cold live-browser acceptance. The ownership receipt at `<Host git dir>/dsh-right-sidebar.patch-state` records the exact patch SHA, Host SHA, region list and catalog limitation. Uninstall may reverse only that exact still-owned patch, must preserve drift, and must regenerate shared catalogs from remaining contributions. The supplied complete setup/uninstall scripts encounter the known global Cordis API generator partition violation on this Host; do not claim lifecycle acceptance or run their destructive portions blindly.
