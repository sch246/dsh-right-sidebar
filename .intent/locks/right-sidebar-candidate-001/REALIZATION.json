{
  "$schema": "dsh-right-sidebar/realization-manifest/0.1",
  "source": {
    "uri": "https://github.com/sch246/dsh-right-sidebar.git",
    "commit": "6b2b58863f0c6d5c94000a0e3be54f23227ce2dc"
  },
  "installation": {
    "build": "DSH_CHECKOUT=/root/deepseek-harness bash scripts/build.sh",
    "buildEnvironment": "A prepared Harness development checkout with built vendored dependencies and package-local dependency links; this dependency closure is not yet locked.",
    "profileAdd": "dsh plugin --profile <profile> add <checkout>",
    "profileInspect": "dsh --profile <profile> --dump-config",
    "service": "dsh-web",
    "restartRequiresAuthority": true
  },
  "ownership": {
    "profilePackage": "@dsh-external/dsh-right-sidebar",
    "generatedPaths": [
      "lib",
      "*.tsbuildinfo"
    ],
    "harnessContribution": {
      "baselineCommit": "b150a551b8d465e31e418e1b2eaf5e79bbb7d28e",
      "patchPathAtSourceCommit": "patches/deepseek-harness.patch",
      "applyOnlyAfterCheck": "git apply --check <patch>",
      "scope": "only hunks attributable to this patch"
    }
  },
  "maintenance": {
    "onSourceOrTargetDrift": "Re-investigate current state and re-synthesize the owned contribution; do not silently reuse this candidate.",
    "preserveUnrelatedChanges": true
  },
  "uninstall": {
    "profileRemove": "dsh plugin --profile <profile> remove @dsh-external/dsh-right-sidebar",
    "profileOrphanCleanup": "If the exact package symlink remains after remove, unlink it only after the profile manifest, pnpm lockfile, pnpm list, bundle list, and dump config contain no package reference and the symlink resolves to the owned checkout.",
    "harnessPatchRemoval": "Re-synthesize the current Harness target without this patch; reverse-apply only after proving owned hunks are unchanged and unrelated later changes are preserved.",
    "deleteSourceCheckout": false
  },
  "knownLimits": [
    "The exact plugin commit was installed from a local Git worktree, not retrieved afresh through the published remote URI.",
    "The build environment and prepared dependency outputs are not immutably bound by this lock.",
    "The DSH/pnpm remove path leaves an inactive orphan link that requires guarded realization-owned cleanup.",
    "The separate Harness target is described here but is not a second mechanically validated protocol target.",
    "Browser behavior and external tab/session-synchronization fixtures were not exercised by the lifecycle experiment."
  ]
}
