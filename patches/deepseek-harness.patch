diff --git a/packages/client/store/src/contract.ts b/packages/client/store/src/contract.ts
index 38e39da024..1dddf35001 100644
--- a/packages/client/store/src/contract.ts
+++ b/packages/client/store/src/contract.ts
@@ -50,0 +51,10 @@ export type BakedActions<T, A extends ActionsDecl<T>> = {
+// @meta-intent:begin dsh-right-sidebar (purpose: Declare the durable subset used by the sidebar layout store.)
+/** Persistence declaration for selected top-level fields of an object store. */
+export interface StorePersistence<T> {
+  /** localStorage key; session-scoped instances append their scope key. */
+  name: string
+  /** Top-level state keys that survive a new store instance. */
+  keys: readonly (keyof T)[]
+}
+// @meta-intent:end dsh-right-sidebar
+
@@ -53 +63 @@ export type BakedActions<T, A extends ActionsDecl<T>> = {
- * gets a fresh state), optional persistence key (mechanical, framework-run),
+ * gets a fresh state), optional whole-store or selected-field persistence,
@@ -58 +68,3 @@ export interface StoreSpec<T, A extends ActionsDecl<T>> {
-  persist?: string
+  // @meta-intent:begin dsh-right-sidebar (purpose: Accept whole-store persistence or an explicit durable field subset.)
+  persist?: string | StorePersistence<T>
+  // @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/store/src/index.ts b/packages/client/store/src/index.ts
index 09414e95e6..34846379a0 100644
--- a/packages/client/store/src/index.ts
+++ b/packages/client/store/src/index.ts
@@ -23 +23 @@ export type {
-  StoreHandle, StoreInstance, StoreSpec,
+  StoreHandle, StoreInstance, StorePersistence, StoreSpec,
@@ -104 +104,5 @@ export function createSnapshotStore<T>(
-  init: T, opts?: { flush?: 'raf' | 'sync'; persist?: { name: string } }): SnapshotStore<T> {
+  init: T,
+  // @meta-intent:begin dsh-right-sidebar (purpose: Allow one store to persist selected fields without reviving transient sidebar state.)
+  opts?: { flush?: 'raf' | 'sync'; persist?: { name: string; keys?: readonly (keyof T)[] } },
+  // @meta-intent:end dsh-right-sidebar
+): SnapshotStore<T> {
@@ -109 +113,3 @@ export function createSnapshotStore<T>(
-  if (opts?.persist) attachPersistence(api, opts.persist.name)
+  // @meta-intent:begin dsh-right-sidebar (purpose: Pass the field selection to the persistence engine.)
+  if (opts?.persist) attachPersistence(api, opts.persist)
+  // @meta-intent:end dsh-right-sidebar
@@ -146 +152,6 @@ export function createSnapshotStore<T>(
-function attachPersistence<T>(api: StoreApi<T>, name: string): void {
+// @meta-intent:begin dsh-right-sidebar (purpose: Overlay durable width onto transient defaults and serialize only selected fields.)
+function attachPersistence<T>(
+  api: StoreApi<T>,
+  options: { name: string; keys?: readonly (keyof T)[] },
+): void {
+  const { name, keys } = options
@@ -154 +165,14 @@ function attachPersistence<T>(api: StoreApi<T>, name: string): void {
-      api.setState(devFreeze(JSON.parse(raw) as T), true)
+      const stored = JSON.parse(raw) as T
+      if (keys === undefined) {
+        api.setState(devFreeze(stored), true)
+      } else {
+        const initial = api.getState()
+        if (typeof initial !== 'object' || initial === null || typeof stored !== 'object' || stored === null) {
+          throw new TypeError(`snapshot store '${name}' keyed persistence requires object state`)
+        }
+        const revived = { ...initial } as T
+        for (const key of keys) {
+          if (Object.hasOwn(stored, key)) revived[key] = stored[key]
+        }
+        api.setState(devFreeze(revived), true)
+      }
@@ -161 +185,7 @@ function attachPersistence<T>(api: StoreApi<T>, name: string): void {
-      localStorage.setItem(name, JSON.stringify(state))
+      if (keys === undefined) {
+        localStorage.setItem(name, JSON.stringify(state))
+      } else {
+        const durable: Partial<T> = {}
+        for (const key of keys) durable[key] = state[key]
+        localStorage.setItem(name, JSON.stringify(durable))
+      }
@@ -166,0 +197 @@ function attachPersistence<T>(api: StoreApi<T>, name: string): void {
+// @meta-intent:end dsh-right-sidebar
@@ -214 +245 @@ export interface EngineStoreHandle<T, A extends ActionsDecl<T>> extends StoreHan
- * @param decl - init lambda (fresh state per instance), optional persist key, actions table.
+ * @param decl - init lambda, optional persistence declaration, and actions table.
@@ -222 +253,3 @@ export function defineStore<T, A extends ActionsDecl<T>>(
-      const persistKey = decl.persist === undefined
+      // @meta-intent:begin dsh-right-sidebar (purpose: Resolve whole-store and selected-field persistence into one scoped storage identity.)
+      const persistName = typeof decl.persist === 'string' ? decl.persist : decl.persist?.name
+      const persistKey = persistName === undefined
@@ -224 +257,2 @@ export function defineStore<T, A extends ActionsDecl<T>>(
-        : scopeKey === undefined ? decl.persist : `${decl.persist}.${scopeKey}`
+        : scopeKey === undefined ? persistName : `${persistName}.${scopeKey}`
+      const persistKeys = typeof decl.persist === 'object' ? decl.persist.keys : undefined
@@ -227 +261,4 @@ export function defineStore<T, A extends ActionsDecl<T>>(
-        persistKey !== undefined ? { persist: { name: persistKey } } : undefined)
+        persistKey === undefined
+          ? undefined
+          : { persist: persistKeys === undefined ? { name: persistKey } : { name: persistKey, keys: persistKeys } })
+      // @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
index 73c5fccb73..bed50452af 100644
--- a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
+++ b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
@@ -40 +40,3 @@
-  padding: 12px 28px 0 20px;
+  /* @meta-intent:begin dsh-right-sidebar (purpose: Keep conversation header utilities clear of the global sidebar control.) */
+  padding: 12px max(28px, var(--dsh-shell-navbar-clearance, 28px)) 0 20px;
+  /* @meta-intent:end dsh-right-sidebar */
diff --git a/packages/client/ui-layout/src/client/AppFrame.module.css b/packages/client/ui-layout/src/client/AppFrame.module.css
index ae56780133..2908eb1517 100644
--- a/packages/client/ui-layout/src/client/AppFrame.module.css
+++ b/packages/client/ui-layout/src/client/AppFrame.module.css
@@ -51,3 +51,3 @@
-/* Drag handles are frame children (columns clip overflow): an 8px hit strip
-   centered on the column border via inline left, above column content. Details
-   adds a visible 12x32 pill at vertical center; sidebar keeps only the hit strip. */
+/* @meta-intent:begin dsh-right-sidebar (purpose: Use the native full-height divider without a second floating sidebar grip.) */
+/* Drag handles are frame children (columns clip overflow): an invisible 8px
+   full-height hit strip centered on the column border via inline left. */
@@ -78,31 +78 @@
-.handle[data-side='details']::after {
-  content: '';
-  position: absolute;
-  top: 50%;
-  left: 50%;
-  transform: translate(-50%, -50%);
-  width: 12px;
-  height: 32px;
-  border-radius: 10px;
-  box-sizing: border-box;
-  background: var(--dsw-alias-button-floating-fill);
-  border: 1px solid var(--dsw-alias-border-l2-darkmode-thin);
-  /* Hover affordance: the details pill hides until the pointer is over its
-     column, the strip itself, or a drag. */
-  opacity: 0;
-  transition:
-    opacity var(--ds-transition-duration-slow) var(--ds-ease-in-out),
-    background var(--ds-transition-duration-slow) var(--ds-ease-in-out);
-}
-
-.detailsCol:hover ~ .handle[data-side='details']::after,
-.handle[data-side='details']:hover::after,
-.handle[data-side='details'][data-dragging='true']::after {
-  opacity: 1;
-}
-
-.handle[data-side='details']:hover::after,
-.handle[data-side='details'][data-dragging='true']::after {
-  background: var(--dsw-alias-button-floating-hover);
-  border-color: var(--dsw-alias-border-l3);
-}
+/* @meta-intent:end dsh-right-sidebar */
@@ -119,0 +90,13 @@
+
+/* @meta-intent:begin dsh-right-sidebar (purpose: Provide a stable application-control seat for the global sidebar toggle.) */
+.navbar {
+  position: absolute;
+  z-index: 30;
+  top: 12px;
+  right: 8px;
+  display: flex;
+  align-items: center;
+  gap: 4px;
+  pointer-events: auto;
+}
+/* @meta-intent:end dsh-right-sidebar */
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index cbef893aed..e9d801a4b8 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -13,2 +13,4 @@
-import { useCallback, useEffect, useLayoutEffect, useRef, useState } from 'react'
-import type { ReactNode } from 'react'
+import { useCallback, useEffect, useRef, useState } from 'react'
+// @meta-intent:begin dsh-right-sidebar (purpose: Type the navbar clearance variable and render seat added for the global sidebar control.)
+import type { CSSProperties, ReactNode } from 'react'
+// @meta-intent:end dsh-right-sidebar
@@ -26 +28,3 @@ export type AppFrameProps =
-  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.overlay'>
+  // @meta-intent:begin dsh-right-sidebar (purpose: Deliver the global navbar action seat to the root frame.)
+  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.navbar.action' | 'shell.overlay'>
+  // @meta-intent:end dsh-right-sidebar
@@ -31,2 +35,12 @@ export type AppFrameProps =
-function CenterColumn(props: { children?: ReactNode }) {
-  return <div className={css.centerCol}>{props.children}</div>
+// @meta-intent:begin dsh-right-sidebar (purpose: Keep the mounted center non-interactive while maximized details cover its track.)
+function CenterColumn(props: { children?: ReactNode; inert: boolean }) {
+  const inertAttribute = props.inert ? { inert: '' } : {}
+  return (
+    <div
+      className={css.centerCol}
+      aria-hidden={props.inert || undefined}
+      {...inertAttribute}
+    >
+      {props.children}
+    </div>
+  )
@@ -33,0 +48 @@ function CenterColumn(props: { children?: ReactNode }) {
+// @meta-intent:end dsh-right-sidebar
@@ -100,4 +114,0 @@ export function AppFrame({
-  const detailsSession = useSessions((s) => {
-    const current = s.current
-    return current !== undefined && s.byId[current]?.blank === false ? current : undefined
-  })
@@ -109,0 +121,4 @@ export function AppFrame({
+  // @meta-intent:begin dsh-right-sidebar (purpose: Measure the global control so conversation utilities reserve its occupied width.)
+  const navbarRef = useRef<HTMLDivElement | null>(null)
+  const [navbarWidth, setNavbarWidth] = useState(28)
+  // @meta-intent:end dsh-right-sidebar
@@ -111,8 +126,3 @@ export function AppFrame({
-  const lastSession = useRef(detailsSession)
-  useLayoutEffect(() => {
-    if (detailsSession === undefined) return
-    if (lastSession.current !== undefined && lastSession.current !== detailsSession) {
-      actions.closeDetails()
-    }
-    lastSession.current = detailsSession
-  }, [actions, detailsSession])
+  // @meta-intent:begin dsh-right-sidebar (purpose: Keep layout visibility independent from session-owned workbench contents.)
+  // Session changes replace details content without changing its open preference.
+  // @meta-intent:end dsh-right-sidebar
@@ -122,0 +133 @@ export function AppFrame({
+    const navbar = navbarRef.current
@@ -130,0 +142,4 @@ export function AppFrame({
+        // @meta-intent:begin dsh-right-sidebar (purpose: Update clearance when registered application controls change width.)
+        const nextNavbarWidth = navbar?.getBoundingClientRect().width ?? 0
+        if (nextNavbarWidth > 0) setNavbarWidth(nextNavbarWidth)
+        // @meta-intent:end dsh-right-sidebar
@@ -133,0 +149,3 @@ export function AppFrame({
+    // @meta-intent:begin dsh-right-sidebar (purpose: Observe the application-control seat independently from the frame.)
+    if (navbar !== null) observer.observe(navbar)
+    // @meta-intent:end dsh-right-sidebar
@@ -152 +170,9 @@ export function AppFrame({
-  const cols = computeColumns(viewport, sidebarPreference, detailsSession === undefined ? 0 : panels.details)
+  // @meta-intent:begin dsh-right-sidebar (purpose: Resolve transient open state against the remembered right-column width.)
+  const detailsPreference = panels.detailsOpen ? panels.detailsWidth : 0
+  const detailsMaximized = panels.detailsOpen && panels.detailsMaximized
+  const cols = computeColumns(viewport, sidebarPreference, detailsPreference, detailsMaximized)
+  const shellNavbarClearance = cols.details === 0 ? Math.max(28, navbarWidth + 16) : 28
+  useEffect(() => {
+    if (panels.detailsOpen && cols.details === 0) actions.closeDetails()
+  }, [actions, cols.details, panels.detailsOpen])
+  // @meta-intent:end dsh-right-sidebar
@@ -179 +205,9 @@ export function AppFrame({
-      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      style={{
+        gridTemplateColumns: detailsMaximized
+          ? `${cols.sidebar}px 0px ${cols.details}px`
+          : `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px`,
+        // @meta-intent:begin dsh-right-sidebar (purpose: Project measured navbar width as a shell CSS variable.)
+        '--dsh-shell-navbar-clearance': `${shellNavbarClearance}px`,
+        '--dsh-shell-navbar-width': `${navbarWidth + 16}px`,
+        // @meta-intent:end dsh-right-sidebar
+      } as CSSProperties}
@@ -181,0 +216 @@ export function AppFrame({
+      data-details-maximized={detailsMaximized || undefined}
@@ -205 +240 @@ export function AppFrame({
-        <CenterColumn>{renderSlot('conversation', {})}</CenterColumn>
+        <CenterColumn inert={detailsMaximized}>{renderSlot('conversation', {})}</CenterColumn>
@@ -212,0 +248,8 @@ export function AppFrame({
+      {/* @meta-intent:begin dsh-right-sidebar (purpose: Render the additive global sidebar control with resolved details visibility.) */}
+      <div ref={navbarRef} className={css.navbar} data-shell-navbar>
+        {renderSlot('shell.navbar.action', {
+          detailsOpen: cols.details > 0,
+          detailsMaximized: panels.detailsMaximized,
+        })}
+      </div>
+      {/* @meta-intent:end dsh-right-sidebar */}
@@ -215 +258 @@ export function AppFrame({
-      {cols.details > 0 && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
+      {cols.details > 0 && !detailsMaximized && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
diff --git a/packages/client/ui-layout/src/client/columns.ts b/packages/client/ui-layout/src/client/columns.ts
index 374ce64703..97730b5ef7 100644
--- a/packages/client/ui-layout/src/client/columns.ts
+++ b/packages/client/ui-layout/src/client/columns.ts
@@ -3,3 +3,6 @@
- * Chain order is fixed by contract: keep center >= CENTER_MIN by shrinking
- * details, then auto-closing it (derived zero width — preferred width
- * preferences are never rewritten, so widening the window restores them).
+ * Normal-mode chain order is fixed by contract: keep center >= CENTER_MIN by
+ * shrinking details, then auto-closing it (derived zero width — preferred
+ * width preferences are never rewritten, so widening the window restores
+ * them). Normal details have no fixed ceiling: the viewport, retained sidebar,
+ * and center floor determine their rendered maximum. Maximized details retain
+ * the resolved sidebar, set center to zero, and occupy the remaining viewport.
@@ -36,2 +38,0 @@ export const DETAILS_MIN = 300
-/** Details drag clamp ceiling. */
-export const DETAILS_MAX = 520
@@ -56 +57,2 @@ export function clampWidth(px: number, min: number, max: number): number {
- * store boundary and callers may still supply stale ranges.
+ * store boundary and callers may still supply stale sidebar values or details
+ * values below the minimum.
@@ -59,0 +62 @@ export function clampWidth(px: number, min: number, max: number): number {
+ * @param detailsMaximized - whether open details occupy all space after the sidebar.
@@ -62 +65,6 @@ export function clampWidth(px: number, min: number, max: number): number {
-export function computeColumns(viewport: number, sidebar: number, details: number): Columns {
+export function computeColumns(
+  viewport: number,
+  sidebar: number,
+  details: number,
+  detailsMaximized = false,
+): Columns {
@@ -65 +73,6 @@ export function computeColumns(viewport: number, sidebar: number, details: numbe
-  const d0 = details === 0 ? 0 : clampWidth(details, DETAILS_MIN, DETAILS_MAX)
+  // @meta-intent:begin dsh-right-sidebar (purpose: Let ordinary details grow to the center floor and maximize across the center.)
+  if (details !== 0 && detailsMaximized) {
+    return { sidebar: s, center: 0, details: Math.max(0, viewport - s) }
+  }
+  const d0 = details === 0 ? 0 : Math.max(DETAILS_MIN, Math.round(details))
+  // @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/ui-layout/src/client/index.ts b/packages/client/ui-layout/src/client/index.ts
index 56afec15e3..e9ae69fa5a 100644
--- a/packages/client/ui-layout/src/client/index.ts
+++ b/packages/client/ui-layout/src/client/index.ts
@@ -4 +4 @@
- * four child slots (declaration = exclusive render authority), seats the
+ * five child slots (declaration = exclusive render authority), seats the
@@ -39 +39 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
-    // there); these four are the frame's children, declared by the same
+    // there); these five are the frame's children, declared by the same
@@ -75,0 +76,4 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
+    // @meta-intent:begin dsh-right-sidebar (purpose: Declare an additive root-scoped seat for the global sidebar control.)
+    /** Application-level controls anchored at the frame's top-right corner. */
+    'shell.navbar.action': { kind: 'list'; scope: 'root'; owner: NavbarActionOwnerProps }
+    // @meta-intent:end dsh-right-sidebar
@@ -109,0 +114,10 @@ export interface DetailsOwnerProps {}
+// @meta-intent:begin dsh-right-sidebar (purpose: Expose resolved visibility to navbar occupants while writes stay on ctx.layout.)
+/** Application-control owner share supplied by the frame. */
+export interface NavbarActionOwnerProps {
+  /** Whether the details column currently occupies layout width. */
+  detailsOpen: boolean
+  /** Whether details use the maximized layout when open. */
+  detailsMaximized: boolean
+}
+// @meta-intent:end dsh-right-sidebar
+
@@ -115 +129 @@ export const inject = ['slots', 'theme', 'locale']
- * into 'root' with the four child-slot declarations, the layout store seat,
+ * into 'root' with the five child-slot declarations, the layout store seat,
@@ -129,0 +144,3 @@ export function apply(ctx: ClientContext): void {
+        // @meta-intent:begin dsh-right-sidebar (purpose: Register the global navbar action child with the root frame.)
+        'shell.navbar.action': { kind: 'list', scope: 'root' },
+        // @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/ui-layout/src/client/service.ts b/packages/client/ui-layout/src/client/service.ts
index 2b2e50e5d5..24cb142be3 100644
--- a/packages/client/ui-layout/src/client/service.ts
+++ b/packages/client/ui-layout/src/client/service.ts
@@ -29,0 +30,4 @@ export interface ILayout {
+  // @meta-intent:begin dsh-right-sidebar (purpose: Expose the right-column maximization action to its navbar control.)
+  /** Toggle whether details occupy all space after the sidebar when open. */
+  toggleDetailsMaximized(): void
+  // @meta-intent:end dsh-right-sidebar
@@ -61,0 +66,7 @@ export class LayoutController implements ILayout {
+  // @meta-intent:begin dsh-right-sidebar (purpose: Forward right-column maximization through the layout service.)
+  /** Toggle whether details occupy all space after the sidebar when open. */
+  toggleDetailsMaximized(): void {
+    this.#require().toggleDetailsMaximized()
+  }
+  // @meta-intent:end dsh-right-sidebar
+
diff --git a/packages/client/ui-layout/src/client/stores.ts b/packages/client/ui-layout/src/client/stores.ts
index 7b6e2807ee..3e048a08d5 100644
--- a/packages/client/ui-layout/src/client/stores.ts
+++ b/packages/client/ui-layout/src/client/stores.ts
@@ -2,3 +2,3 @@
- * The root entry's transient layout store: panel geometry as plain widths in
- * px (0 = closed). Module level exports the factory only — a module-level
- * handle would pin the store's identity in the module
+ * The root entry's layout store: transient panel state plus durable details
+ * width and maximization preferences. Module level exports the factory only —
+ * a module-level handle would pin the store's identity in the module
@@ -12 +12 @@ import {
-  clampWidth, DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
+  clampWidth, DETAILS_DEFAULT, DETAILS_MIN,
@@ -17,2 +17,2 @@ import {
- * Layout store state: panel width preferences in px (0 = closed), plus the
- * narrow-viewport pair — `narrow` mirrors AppFrame's breakpoint reading
+ * Layout store state: panel preferences, transient details visibility, plus
+ * the narrow-viewport pair — `narrow` mirrors AppFrame's breakpoint reading
@@ -23 +23,10 @@ import {
-type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowExpanded: boolean }
+// @meta-intent:begin dsh-right-sidebar (purpose: Separate remembered right-column width from transient visibility.)
+type LayoutState = {
+  sidebar: number
+  detailsWidth: number
+  detailsOpen: boolean
+  detailsMaximized: boolean
+  narrow: boolean
+  narrowExpanded: boolean
+}
+// @meta-intent:end dsh-right-sidebar
@@ -35,0 +45 @@ type LayoutActions = {
+  toggleDetailsMaximized: (draft: LayoutState) => void
@@ -39,5 +49,3 @@ type LayoutActions = {
- * Create the layout panel store handle. The preference IS the width, so
- * closing a panel forgets its drag width — reopening restores the contract
- * default. Actions are the complete write set: drag writes clamp
- * into the panel's contract range and never cross the open/closed line;
- * open/close transitions write 0 / the default explicitly. Below the
+ * Create the layout panel store handle. Right-column width, visibility, and
+ * maximization are separate: dragging changes durable width, open and close
+ * only change transient visibility, and maximization survives close. Below the
@@ -48,0 +57 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
+  // @meta-intent:begin dsh-right-sidebar (purpose: Persist details preferences while keeping open/close state fresh on every load.)
@@ -50 +59,9 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
-    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false }),
+    init: (): LayoutState => ({
+      sidebar: SIDEBAR_DEFAULT,
+      detailsWidth: DETAILS_DEFAULT,
+      detailsOpen: false,
+      detailsMaximized: false,
+      narrow: false,
+      narrowExpanded: false,
+    }),
+    persist: { name: 'dsh.layout.panels.v1', keys: ['detailsWidth', 'detailsMaximized'] },
@@ -53 +70 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
-      setDetails: (d, px: number) => { d.details = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
+      setDetails: (d, px: number) => { d.detailsWidth = Math.max(DETAILS_MIN, Math.round(px)) },
@@ -67,2 +84,3 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
-      openDetails: (d) => { if (d.details === 0) d.details = DETAILS_DEFAULT },
-      closeDetails: (d) => { d.details = 0 },
+      openDetails: (d) => { d.detailsOpen = true },
+      closeDetails: (d) => { d.detailsOpen = false },
+      toggleDetailsMaximized: (d) => { d.detailsMaximized = !d.detailsMaximized },
@@ -70,0 +89 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
+  // @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index a640480360..a65ee575c1 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -126,0 +127 @@ beforeEach(() => {
+  localStorage.clear()
@@ -213 +214 @@ describe('AppFrame', () => {
-  it('ignores unselected states and closes only when the Session id changes', () => {
+  it('a Session id change retains visibility, width and maximization', () => {
@@ -215,4 +216,5 @@ describe('AppFrame', () => {
-    expect(tracks(frame)).toEqual([280, 0])
-
-    act(() => { instance.actions.openDetails() })
-    expect(tracks(frame)).toEqual([280, 360])
+    act(() => {
+      instance.actions.openDetails()
+      instance.actions.toggleDetailsMaximized()
+    })
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
@@ -222 +224,2 @@ describe('AppFrame', () => {
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+    expect(instance.getSnapshot()).toMatchObject({ detailsOpen: true, detailsMaximized: true })
@@ -224,0 +228 @@ describe('AppFrame', () => {
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
@@ -228,7 +232,7 @@ describe('AppFrame', () => {
-    expect(tracks(frame)).toEqual([280, 0])
-    expect(instance.getSnapshot().details).toBe(360)
-
-    selectedSession.current = 's-next' as SessionId
-    selectedSessionBlank.current = false
-    act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 360])
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+    expect(instance.getSnapshot()).toMatchObject({
+      detailsWidth: 360,
+      detailsOpen: true,
+      detailsMaximized: true,
+    })
+  })
@@ -235,0 +240 @@ describe('AppFrame', () => {
+  it('an open no-session layout stays open when the first Session materializes', () => {
@@ -236,0 +242,8 @@ describe('AppFrame', () => {
+    const { frame, instance, rerenderFrame } = mountFrame()
+    act(() => {
+      instance.actions.toggleDetailsMaximized()
+      instance.actions.openDetails()
+    })
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+
+    selectedSession.current = 's-first' as SessionId
@@ -238,4 +251,2 @@ describe('AppFrame', () => {
-    expect(tracks(frame)).toEqual([280, 0])
-    selectedSession.current = 's-test' as SessionId
-    act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+    expect(instance.getSnapshot()).toMatchObject({ detailsOpen: true, detailsMaximized: true })
@@ -248 +259 @@ describe('AppFrame', () => {
-    expect(instance.getSnapshot().details).toBe(0)
+    expect(instance.getSnapshot()).toMatchObject({ detailsWidth: 360, detailsOpen: false })
@@ -274,0 +286,9 @@ describe('AppFrame', () => {
+  it('normal details drag has no fixed ceiling before the center floor', () => {
+    const { frame, instance } = mountFrame()
+    act(() => { instance.actions.openDetails() })
+    const handles = frame.querySelectorAll('[class*="handle"]')
+    drag(handles[1]!, 1560, 800)
+    expect(tracks(frame)[1]).toBe(1000)
+    expect(instance.getSnapshot().detailsWidth).toBe(1120)
+  })
+
@@ -282 +302 @@ describe('AppFrame', () => {
-    expect(instance.getSnapshot().details).toBe(320)
+    expect(instance.getSnapshot().detailsWidth).toBe(320)
@@ -322,0 +343,32 @@ describe('AppFrame', () => {
+
+  it('maximized details retain the sidebar, hide and inert the mounted center, and omit the details handle', () => {
+    const { frame, instance, getByTestId, slotCalls } = mountFrame()
+    act(() => {
+      instance.actions.openDetails()
+      instance.actions.toggleDetailsMaximized()
+    })
+
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+    expect(frame.hasAttribute('data-details-maximized')).toBe(true)
+    const center = getByTestId('center-content').parentElement!
+    expect(center.hasAttribute('inert')).toBe(true)
+    expect(center.getAttribute('aria-hidden')).toBe('true')
+    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
+    expect(slotCalls.filter(c => c.key === 'shell.navbar.action').at(-1)!.props).toEqual({
+      detailsOpen: true,
+      detailsMaximized: true,
+    })
+
+    act(() => { instance.actions.closeDetails() })
+    expect(tracks(frame)).toEqual([280, 0])
+    expect(center.hasAttribute('inert')).toBe(false)
+    expect(center.hasAttribute('aria-hidden')).toBe(false)
+    expect(instance.getSnapshot()).toMatchObject({ detailsOpen: false, detailsMaximized: true })
+    expect(slotCalls.filter(c => c.key === 'shell.navbar.action').at(-1)!.props).toEqual({
+      detailsOpen: false,
+      detailsMaximized: true,
+    })
+
+    act(() => { instance.actions.openDetails() })
+    expect(frame.style.gridTemplateColumns).toBe('280px 0px 1640px')
+  })
diff --git a/packages/client/ui-layout/tests/apply.client.spec.ts b/packages/client/ui-layout/tests/apply.client.spec.ts
index e546294445..8c81c64c4a 100644
--- a/packages/client/ui-layout/tests/apply.client.spec.ts
+++ b/packages/client/ui-layout/tests/apply.client.spec.ts
@@ -37 +37 @@ describe('ui-layout client apply', () => {
-  it('provides ctx.layout and registers AppFrame into root with the three child declarations', async () => {
+  it('provides ctx.layout and registers AppFrame into root with its child declarations', async () => {
@@ -44 +44 @@ describe('ui-layout client apply', () => {
-    // …and declared the three children in the ledger.
+    // …and declared its children in the ledger.
@@ -47,0 +48,2 @@ describe('ui-layout client apply', () => {
+    expect(slots.spec('shell.navbar.action')).toEqual({ kind: 'list', scope: 'root' })
+    expect(slots.spec('shell.overlay')).toEqual({ kind: 'list', scope: 'root' })
@@ -55,0 +58 @@ describe('ui-layout client apply', () => {
+      toggleDetailsMaximized: vi.fn(),
@@ -60,0 +64 @@ describe('ui-layout client apply', () => {
+    layout.toggleDetailsMaximized()
@@ -61,0 +66 @@ describe('ui-layout client apply', () => {
+    expect(actions.toggleDetailsMaximized).toHaveBeenCalledOnce()
@@ -96,0 +102 @@ describe('ui-layout client apply', () => {
+    expect(slots.spec('shell.navbar.action')).toBeUndefined()
diff --git a/packages/client/ui-layout/tests/columns.client.spec.ts b/packages/client/ui-layout/tests/columns.client.spec.ts
index ae8c39a117..908d616de8 100644
--- a/packages/client/ui-layout/tests/columns.client.spec.ts
+++ b/packages/client/ui-layout/tests/columns.client.spec.ts
@@ -30 +30 @@ describe('computeColumns', () => {
-  it('preferences beyond the clamp range are clamped before solving', () => {
+  it('sidebar and details minima are enforced before solving', () => {
@@ -35,0 +36,19 @@ describe('computeColumns', () => {
+
+  it('normal details have no fixed ceiling beyond the retained sidebar and center floor', () => {
+    expect(computeColumns(2500, open(SIDEBAR_DEFAULT), open(1500)))
+      .toEqual({ sidebar: SIDEBAR_DEFAULT, center: 720, details: 1500 })
+    expect(computeColumns(2200, open(SIDEBAR_DEFAULT), open(9999)))
+      .toEqual({ sidebar: SIDEBAR_DEFAULT, center: CENTER_MIN, details: 1280 })
+  })
+
+  it('maximized details retain the sidebar and occupy the remaining viewport', () => {
+    expect(computeColumns(1920, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), true))
+      .toEqual({ sidebar: SIDEBAR_DEFAULT, center: 0, details: 1920 - SIDEBAR_DEFAULT })
+    expect(computeColumns(1000, closed(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), true))
+      .toEqual({ sidebar: SIDEBAR_COLLAPSED, center: 0, details: 1000 - SIDEBAR_COLLAPSED })
+  })
+
+  it('a remembered maximized preference does not open closed details', () => {
+    expect(computeColumns(1920, open(SIDEBAR_DEFAULT), closed(DETAILS_DEFAULT), true))
+      .toEqual({ sidebar: SIDEBAR_DEFAULT, center: 1920 - SIDEBAR_DEFAULT, details: 0 })
+  })
diff --git a/packages/client/ui-layout/tests/layout-store.client.spec.ts b/packages/client/ui-layout/tests/layout-store.client.spec.ts
index c6f0069197..2ecbda40c4 100644
--- a/packages/client/ui-layout/tests/layout-store.client.spec.ts
+++ b/packages/client/ui-layout/tests/layout-store.client.spec.ts
@@ -3,2 +3,2 @@
- * createLayoutStore unit account: init shape, the action write set (clamp
- * inside actions), and the absence of browser persistence. Uses the
+ * createLayoutStore unit account: init state, the action write set (clamp
+ * inside actions), and selective browser persistence. Uses the
@@ -11 +11 @@ import {
-  DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
+  DETAILS_DEFAULT, DETAILS_MIN,
@@ -15 +15 @@ import {
-const PERSIST_KEY = 'dsh.layout.panels'
+const PERSIST_KEY = 'dsh.layout.panels.v1'
@@ -22 +22,8 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot()).toEqual({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false })
+    expect(store.getSnapshot()).toEqual({
+      sidebar: SIDEBAR_DEFAULT,
+      detailsWidth: DETAILS_DEFAULT,
+      detailsOpen: false,
+      detailsMaximized: false,
+      narrow: false,
+      narrowExpanded: false,
+    })
@@ -32 +39 @@ describe('createLayoutStore', () => {
-  it('setSidebar/setDetails clamp into the contract ranges', () => {
+  it('setSidebar clamps to its range while details has only a minimum', () => {
@@ -39 +46 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot().details).toBe(DETAILS_MIN)
+    expect(store.getSnapshot().detailsWidth).toBe(DETAILS_MIN)
@@ -41 +48 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot().details).toBe(DETAILS_MAX)
+    expect(store.getSnapshot().detailsWidth).toBe(9999)
@@ -58 +65 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot()).toEqual({ sidebar: 400, details: 0, narrow: true, narrowExpanded: true })
+    expect(store.getSnapshot()).toMatchObject({ sidebar: 400, narrow: true, narrowExpanded: true })
@@ -77 +84 @@ describe('createLayoutStore', () => {
-  it('openDetails uses the contract default, preserves an open width, and closeDetails zeroes', () => {
+  it('open and close change only transient visibility', () => {
@@ -80 +87 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot().details).toBe(DETAILS_DEFAULT)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: DETAILS_DEFAULT, detailsOpen: true })
@@ -83 +90 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot().details).toBe(500)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: 500, detailsOpen: true })
@@ -85 +92 @@ describe('createLayoutStore', () => {
-    expect(store.getSnapshot().details).toBe(0)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: 500, detailsOpen: false })
@@ -88 +95,11 @@ describe('createLayoutStore', () => {
-  it('does not persist panel geometry', () => {
+  it('close preserves maximization and reopening restores it', () => {
+    const { store, actions } = createLayoutStore().create()
+    actions.openDetails()
+    actions.toggleDetailsMaximized()
+    actions.closeDetails()
+    expect(store.getSnapshot()).toMatchObject({ detailsOpen: false, detailsMaximized: true })
+    actions.openDetails()
+    expect(store.getSnapshot()).toMatchObject({ detailsOpen: true, detailsMaximized: true })
+  })
+
+  it('persists only details width and maximization', () => {
@@ -93 +110,5 @@ describe('createLayoutStore', () => {
-    expect(localStorage.getItem(PERSIST_KEY)).toBeNull()
+    first.actions.toggleDetailsMaximized()
+    expect(JSON.parse(localStorage.getItem(PERSIST_KEY)!)).toEqual({
+      detailsWidth: 500,
+      detailsMaximized: true,
+    })
@@ -98 +119,3 @@ describe('createLayoutStore', () => {
-      details: 0,
+      detailsWidth: 500,
+      detailsOpen: false,
+      detailsMaximized: true,
diff --git a/packages/client/ui-layout/tests/service.client.spec.ts b/packages/client/ui-layout/tests/service.client.spec.ts
index ea4118927c..724d169276 100644
--- a/packages/client/ui-layout/tests/service.client.spec.ts
+++ b/packages/client/ui-layout/tests/service.client.spec.ts
@@ -12,0 +13 @@ function fakePanels(): PanelActions {
+    toggleDetailsMaximized: vi.fn(),
@@ -17 +18 @@ describe('LayoutController', () => {
-  it('forwards the three panel actions to the attached set', () => {
+  it('forwards the panel actions to the attached set', () => {
@@ -24,0 +26 @@ describe('LayoutController', () => {
+    service.toggleDetailsMaximized()
@@ -28,0 +31 @@ describe('LayoutController', () => {
+    expect(panels.toggleDetailsMaximized).toHaveBeenCalledTimes(1)
@@ -37,0 +41 @@ describe('LayoutController', () => {
+    expect(() => { service.toggleDetailsMaximized() }).toThrow(/panel actions not wired/)
diff --git a/packages/client/ui-slots/src/store.ts b/packages/client/ui-slots/src/store.ts
index 907762b7b4..e67613e8a5 100644
--- a/packages/client/ui-slots/src/store.ts
+++ b/packages/client/ui-slots/src/store.ts
@@ -15,0 +16,3 @@ export type {
+  // @meta-intent:begin dsh-right-sidebar (purpose: Re-export selected-field persistence through the slot store API.)
+  StorePersistence,
+  // @meta-intent:end dsh-right-sidebar
