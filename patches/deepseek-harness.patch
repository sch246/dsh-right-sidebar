diff --git a/packages/client/runtime/src/client/contract/store.ts b/packages/client/runtime/src/client/contract/store.ts
index 0eb7917b03..25a4c38b05 100644
--- a/packages/client/runtime/src/client/contract/store.ts
+++ b/packages/client/runtime/src/client/contract/store.ts
@@ -19,9 +19,11 @@ import type {
 
 // Store contract types are ui-slots authority; re-exported beside the engine
 // so store consumers get one import path.
+// @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Expose keyed persistence so sidebar width survives while visibility stays transient."}
 export type {
-  ActionsDecl, BakedActions, BoundActions, StoreFactory, StoreHandle, StoreInstance, StoreSpec,
+  ActionsDecl, BakedActions, BoundActions, StoreFactory, StoreHandle, StoreInstance, StorePersistence, StoreSpec,
 } from '@deepseek-ai/dsh-client-ui-slots'
+// @meta-intent:end dsh-right-sidebar
 
 /** Minimal observable snapshot source: Session objects and snapshot stores both satisfy it. */
 export interface ObservableSnapshot<T> { getSnapshot(): T; subscribe(fn: () => void): () => void }
@@ -84,12 +86,18 @@ function rafBatch(notify: () => void): () => void {
  * @returns the store.
  */
 export function createSnapshotStore<T>(
-  init: T, opts?: { flush?: 'raf' | 'sync'; persist?: { name: string } }): SnapshotStore<T> {
+  init: T,
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Allow one store to persist selected fields without reviving transient sidebar state."}
+  opts?: { flush?: 'raf' | 'sync'; persist?: { name: string; keys?: readonly (keyof T)[] } },
+  // @meta-intent:end dsh-right-sidebar
+): SnapshotStore<T> {
   // Immer enters through produce() in update() below (identical semantics to
   // the immer middleware without its setState-signature mutator generics).
   const withSelector = subscribeWithSelector(() => init)
   const api: StoreApi<T> = createStore<T>()(withSelector)
-  if (opts?.persist) attachPersistence(api, opts.persist.name)
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Pass the keyed declaration to the persistence engine."}
+  if (opts?.persist) attachPersistence(api, opts.persist)
+  // @meta-intent:end dsh-right-sidebar
 
   let subscribe = (fn: () => void) => api.subscribe(fn)
   if (opts?.flush === 'raf') {
@@ -124,7 +132,12 @@ export function createSnapshotStore<T>(
  * because the corruption happens before serialization. Storage failures
  * (quota, private mode) only disable persistence, never break the store.
  */
-function attachPersistence<T>(api: StoreApi<T>, name: string): void {
+// @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Overlay durable width fields onto fresh transient defaults and serialize only those fields."}
+function attachPersistence<T>(
+  api: StoreApi<T>,
+  options: { name: string; keys?: readonly (keyof T)[] },
+): void {
+  const { name, keys } = options
   // Non-browser runs (node e2e booting the client tree) have no localStorage:
   // persistence silently disables — same contract as a storage failure, minus
   // the per-store console noise a ReferenceError would produce.
@@ -132,19 +145,39 @@ function attachPersistence<T>(api: StoreApi<T>, name: string): void {
   try {
     const raw = localStorage.getItem(name)
     if (raw !== null) {
-      api.setState(devFreeze(JSON.parse(raw) as T), true)
+      const stored = JSON.parse(raw) as T
+      if (keys === undefined) {
+        api.setState(devFreeze(stored), true)
+      } else {
+        const initial = api.getState()
+        if (typeof initial !== 'object' || initial === null || typeof stored !== 'object' || stored === null) {
+          throw new TypeError(`snapshot store '${name}' keyed persistence requires object state`)
+        }
+        const revived = { ...initial } as T
+        for (const key of keys) {
+          if (Object.hasOwn(stored, key)) revived[key] = stored[key]
+        }
+        api.setState(devFreeze(revived), true)
+      }
     }
   } catch (error) {
     console.error(`snapshot store '${name}' rehydration failed:`, error)
   }
   api.subscribe((state) => {
     try {
-      localStorage.setItem(name, JSON.stringify(state))
+      if (keys === undefined) {
+        localStorage.setItem(name, JSON.stringify(state))
+      } else {
+        const durable: Partial<T> = {}
+        for (const key of keys) durable[key] = state[key]
+        localStorage.setItem(name, JSON.stringify(durable))
+      }
     } catch (error) {
       console.error(`snapshot store '${name}' persistence failed:`, error)
     }
   })
 }
+// @meta-intent:end dsh-right-sidebar
 
 /** Deep-freeze wholesale-set state outside production: set() bypasses immer's freeze. */
 function devFreeze<T>(value: T): T {
@@ -209,12 +242,19 @@ export function defineStore<T, A extends ActionsDecl<T>>(
   return {
     spec: decl,
     create(scopeKey?: string): EngineStoreInstance<T, A> {
+      // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Resolve string and keyed persistence declarations into one scoped storage identity."}
       const persistKey = decl.persist === undefined
         ? undefined
-        : scopeKey === undefined ? decl.persist : `${decl.persist}.${scopeKey}`
+        : scopeKey === undefined
+          ? typeof decl.persist === 'string' ? decl.persist : decl.persist.name
+          : `${typeof decl.persist === 'string' ? decl.persist : decl.persist.name}.${scopeKey}`
+      const persistKeys = typeof decl.persist === 'object' ? decl.persist.keys : undefined
       const store = createSnapshotStore<T>(
         decl.init(),
-        persistKey !== undefined ? { persist: { name: persistKey } } : undefined)
+        persistKey !== undefined
+          ? { persist: persistKeys === undefined ? { name: persistKey } : { name: persistKey, keys: persistKeys } }
+          : undefined)
+      // @meta-intent:end dsh-right-sidebar
       const actions = {} as Record<string, (...params: unknown[]) => void>
       for (const key of Object.keys(decl.actions)) {
         const mutate = decl.actions[key] as (draft: T, ...params: unknown[]) => void
diff --git a/packages/client/runtime/tests/store.client.spec.ts b/packages/client/runtime/tests/store.client.spec.ts
index 3abd0483f2..455524837c 100644
--- a/packages/client/runtime/tests/store.client.spec.ts
+++ b/packages/client/runtime/tests/store.client.spec.ts
@@ -188,6 +188,31 @@ describe('defineStore', () => {
     expect(backing.has('spec.chat')).toBe(true)
   })
 
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Protect width-only persistence and fresh visibility defaults."}
+  it('persists selected top-level keys and overlays them onto fresh transient defaults', () => {
+    const backing = new Map<string, string>()
+    vi.stubGlobal('localStorage', {
+      getItem: (k: string) => backing.get(k) ?? null,
+      setItem: (k: string, v: string) => { backing.set(k, v) },
+      removeItem: (k: string) => { backing.delete(k) },
+    })
+    const handle = defineStore({
+      init: () => ({ width: 360, open: false, transient: 'fresh' }),
+      persist: { name: 'spec.partial', keys: ['width'] },
+      actions: {
+        setWidth: (d, width: number) => { d.width = width },
+        open: (d) => { d.open = true; d.transient = 'stale' },
+      },
+    })
+    const first = handle.create()
+    first.actions.setWidth(512)
+    first.actions.open()
+    expect(JSON.parse(backing.get('spec.partial')!)).toEqual({ width: 512 })
+    const revived = handle.create()
+    expect(revived.getSnapshot()).toEqual({ width: 512, open: false, transient: 'fresh' })
+  })
+  // @meta-intent:end dsh-right-sidebar
+
   it('clearPersisted is a no-op without a persist declaration or without storage', () => {
     const inst = declare().create('s1')   // no persist key declared
     expect(() => { inst.clearPersisted() }).not.toThrow()
diff --git a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
index 265a0bd802..551221d4a2 100644
--- a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
+++ b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
@@ -28,7 +28,9 @@
 .header {
   position: relative;
   flex: none;
-  padding: 12px 28px 0 20px;
+  /* @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Keep conversation header utilities clear of the global sidebar control."} */
+  padding: 12px max(28px, var(--dsh-shell-navbar-clearance, 28px)) 0 20px;
+  /* @meta-intent:end dsh-right-sidebar */
   border-bottom: 1px solid transparent;
 }
 
diff --git a/packages/client/ui-layout/src/client/AppFrame.module.css b/packages/client/ui-layout/src/client/AppFrame.module.css
index ae56780133..c849b45d80 100644
--- a/packages/client/ui-layout/src/client/AppFrame.module.css
+++ b/packages/client/ui-layout/src/client/AppFrame.module.css
@@ -48,9 +48,9 @@
   border-left: none;
 }
 
-/* Drag handles are frame children (columns clip overflow): an 8px hit strip
-   centered on the column border via inline left, above column content. Details
-   adds a visible 12x32 pill at vertical center; sidebar keeps only the hit strip. */
+/* @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Use the native full-height divider without a second floating sidebar grip."} */
+/* Drag handles are frame children (columns clip overflow): an invisible 8px
+   full-height hit strip centered on the column border via inline left. */
 .handle {
   position: absolute;
   top: 0;
@@ -74,38 +74,7 @@
     transition: none;
   }
 }
-
-.handle[data-side='details']::after {
-  content: '';
-  position: absolute;
-  top: 50%;
-  left: 50%;
-  transform: translate(-50%, -50%);
-  width: 12px;
-  height: 32px;
-  border-radius: 10px;
-  box-sizing: border-box;
-  background: var(--dsw-alias-button-floating-fill);
-  border: 1px solid var(--dsw-alias-border-l2-darkmode-thin);
-  /* Hover affordance: the details pill hides until the pointer is over its
-     column, the strip itself, or a drag. */
-  opacity: 0;
-  transition:
-    opacity var(--ds-transition-duration-slow) var(--ds-ease-in-out),
-    background var(--ds-transition-duration-slow) var(--ds-ease-in-out);
-}
-
-.detailsCol:hover ~ .handle[data-side='details']::after,
-.handle[data-side='details']:hover::after,
-.handle[data-side='details'][data-dragging='true']::after {
-  opacity: 1;
-}
-
-.handle[data-side='details']:hover::after,
-.handle[data-side='details'][data-dragging='true']::after {
-  background: var(--dsw-alias-button-floating-hover);
-  border-color: var(--dsw-alias-border-l3);
-}
+/* @meta-intent:end dsh-right-sidebar */
 
 .overlayLayer {
   position: absolute;
@@ -114,6 +83,19 @@
   pointer-events: none;
 }
 
+/* @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Provide a stable application-control seat for the global sidebar toggle."} */
+.navbar {
+  position: absolute;
+  z-index: 30;
+  top: 12px;
+  right: 8px;
+  display: flex;
+  align-items: center;
+  gap: 4px;
+  pointer-events: auto;
+}
+/* @meta-intent:end dsh-right-sidebar */
+
 .overlayLayer > * {
   pointer-events: auto;
 }
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index 2696dc91fa..4d0f813a81 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -11,7 +11,9 @@
  * zero self-made hooks.
  */
 import { useCallback, useEffect, useLayoutEffect, useRef, useState } from 'react'
-import type { ReactNode } from 'react'
+// @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Type the navbar clearance variable and render seat added for the global sidebar control."}
+import type { CSSProperties, ReactNode } from 'react'
+// @meta-intent:end dsh-right-sidebar
 import type { PropsRenderSlots, PropsRuntime, PropsStore } from '@deepseek-ai/dsh-client-ui-slots'
 import { computeColumns, SIDEBAR_AUTO_COLLAPSE, SIDEBAR_DEFAULT } from './columns.ts'
 import type { createLayoutStore } from './stores.ts'
@@ -20,7 +22,9 @@ import css from './AppFrame.module.css'
 /** Full composed props: runtime share + child-slot render share + store share. */
 export type AppFrameProps =
   & PropsRuntime<'root'>
-  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.overlay'>
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Deliver the global navbar action seat to the root frame."}
+  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.navbar.action' | 'shell.overlay'>
+  // @meta-intent:end dsh-right-sidebar
   & PropsStore<ReturnType<typeof createLayoutStore>>
 
 /** Center column grid item (session-body building block). */
@@ -91,25 +95,29 @@ export function AppFrame({
   renderSlot,
 }: AppFrameProps) {
   const panels = useStore(s => s)
-  const detailsSession = useSessions((s) => {
-    const current = s.current
-    return current !== undefined && s.byId[current]?.blank === false ? current : undefined
-  })
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Keep the right column available before the first turn, close it only on session change, and measure its global control."}
+  const currentSession = useSessions(s => s.current)
   const frameRef = useRef<HTMLDivElement | null>(null)
+  const navbarRef = useRef<HTMLElement | null>(null)
   const [viewport, setViewport] = useState(() => window.innerWidth)
+  const [navbarWidth, setNavbarWidth] = useState(28)
 
-  const lastSession = useRef(detailsSession)
+  const lastSession = useRef(currentSession)
   useLayoutEffect(() => {
-    if (detailsSession === undefined) return
-    if (lastSession.current !== undefined && lastSession.current !== detailsSession) {
+    if (currentSession === undefined) return
+    if (lastSession.current !== undefined && lastSession.current !== currentSession) {
       actions.closeDetails()
     }
-    lastSession.current = detailsSession
-  }, [actions, detailsSession])
+    lastSession.current = currentSession
+  }, [actions, currentSession])
+  // @meta-intent:end dsh-right-sidebar
 
   // Track the frame's own box (not the window): rAF-throttled ResizeObserver.
   useEffect(() => {
     const el = frameRef.current
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Measure the global control so conversation utilities reserve exactly its occupied width."}
+    const navbar = navbarRef.current
+    // @meta-intent:end dsh-right-sidebar
     /* v8 ignore next -- the ref is always attached by effect time: the frame div renders unconditionally. */
     if (el === null) return
     let raf: number | null = null
@@ -118,9 +126,16 @@ export function AppFrame({
         raf = null
         const width = el.getBoundingClientRect().width
         if (width > 0) setViewport(width)
+        // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Update clearance when registered application controls change width."}
+        const nextNavbarWidth = navbar?.getBoundingClientRect().width ?? 0
+        if (nextNavbarWidth > 0) setNavbarWidth(nextNavbarWidth)
+        // @meta-intent:end dsh-right-sidebar
       })
     })
     observer.observe(el)
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Observe the application-control seat independently from the frame."}
+    if (navbar !== null) observer.observe(navbar)
+    // @meta-intent:end dsh-right-sidebar
     return () => {
       observer.disconnect()
       if (raf !== null) cancelAnimationFrame(raf)
@@ -139,9 +154,13 @@ export function AppFrame({
   const sidebarPreference = sidebarCollapsed
     ? 0
     : panels.sidebar === 0 ? SIDEBAR_DEFAULT : panels.sidebar
-  const cols = computeColumns(viewport, sidebarPreference, detailsSession === undefined ? 0 : panels.details)
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Resolve transient open state against the remembered right-column width."}
+  const detailsPreference = panels.detailsOpen ? panels.detailsWidth : 0
+  const cols = computeColumns(viewport, sidebarPreference, detailsPreference)
   const colsRef = useRef(cols)
   colsRef.current = cols
+  const shellNavbarClearance = cols.details === 0 ? Math.max(28, navbarWidth + 16) : 28
+  // @meta-intent:end dsh-right-sidebar
 
   // The drag base is the rendered width captured at drag start (grabbing a
   // concession-clamped panel must not jump back to the stored preference);
@@ -165,7 +184,12 @@ export function AppFrame({
     <div
       ref={frameRef}
       className={css.frame}
-      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      style={{
+        gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px`,
+        // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Project measured navbar width as a shell CSS contract."}
+        '--dsh-shell-navbar-clearance': `${shellNavbarClearance}px`,
+        // @meta-intent:end dsh-right-sidebar
+      } as CSSProperties}
       data-sidebar-collapsed={sidebarCollapsed || undefined}
       data-details-collapsed={cols.details === 0 || undefined}
       data-dragging={dragging || undefined}
@@ -193,9 +217,20 @@ export function AppFrame({
       <div className={css.overlayLayer} data-shell-overlay>
         {renderSlot('shell.overlay', {})}
       </div>
+      {/* @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Render the additive global sidebar control with authoritative details actions."} */}
+      <nav ref={navbarRef} className={css.navbar} aria-label="Application controls" data-shell-navbar>
+        {renderSlot('shell.navbar.action', {
+          detailsOpen: cols.details > 0,
+          openDetails: actions.openDetails,
+          closeDetails: actions.closeDetails,
+        })}
+      </nav>
+      {/* @meta-intent:end dsh-right-sidebar */}
       {/* The collapsed rail is fixed-width: no resize handle while closed. */}
       {!sidebarCollapsed && <DragHandle side="sidebar" left={cols.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
+      {/* @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Expose resize only while the transient right column occupies width."} */}
       {cols.details > 0 && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
+      {/* @meta-intent:end dsh-right-sidebar */}
     </div>
   )
 }
diff --git a/packages/client/ui-layout/src/client/index.ts b/packages/client/ui-layout/src/client/index.ts
index c56d83bfbb..3754a670ee 100644
--- a/packages/client/ui-layout/src/client/index.ts
+++ b/packages/client/ui-layout/src/client/index.ts
@@ -1,7 +1,7 @@
 /**
  * Layout plugin, browser half: one register() call contributes AppFrame into
  * the runtime's built-in 'root' slot and, in the same breath, declares the
- * four child slots (declaration = exclusive render authority), seats the
+ * five child slots (declaration = exclusive render authority), seats the
  * layout store (panel geometry), and wires the panel-action service face.
  * ctx.layout is the cross-plugin panel-action contract; navigation state lives
  * with the runtime sessions service. A second effect seats the theme
@@ -33,9 +33,10 @@ declare module '@deepseek-ai/cordis' {
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface SlotMap {
     // The 'root' entry itself is the runtime's built-in slot (declared
-    // there); these four are the frame's children, declared by the same
+    // there); these five are the frame's children, declared by the same
     // register() call that contributes AppFrame. Session owners never pass
     // sessionId: the framework injects it as a standard prop.
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Declare an additive root-scoped seat for the global sidebar control."}
     /**
      * The whole left column. OCCUPIED by ui-sidebar's SidebarRoot, which
      * declares the workspace and settings seats inside it — registering here
@@ -70,6 +71,13 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
      * `session` scope, and `ctx.layout` owns whether the column is open.
      */
     'details': { kind: 'single'; scope: 'session'; owner: DetailsOwnerProps }
+    /**
+     * Global application-navbar actions. The frame supplies the resolved
+     * details-column visibility and its open/close actions so a root-scoped
+     * control can stay synchronized without mirroring layout state.
+     */
+    'shell.navbar.action': { kind: 'list'; scope: 'root'; owner: NavbarActionOwnerProps }
+    // @meta-intent:end dsh-right-sidebar
     /**
      * Frame-wide floating layer, above every column and outside their scroll
      * containers. Deliberately generic and unowned by any feature: a badge, a
@@ -104,12 +112,24 @@ export interface ConvOwnerProps {}
 /** Details owner share: empty — sessionId arrives as a framework-standard prop. */
 export interface DetailsOwnerProps {}
 
+// @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Expose resolved visibility and authoritative open/close writes to navbar occupants."}
+/** Global navbar action share: real layout state plus the authoritative writes. */
+export interface NavbarActionOwnerProps {
+  /** Whether the right column currently occupies layout width. */
+  detailsOpen: boolean
+  /** Open the right column at its remembered width. */
+  openDetails(): void
+  /** Collapse the right column without discarding its remembered width. */
+  closeDetails(): void
+}
+// @meta-intent:end dsh-right-sidebar
+
 /** Required services (cordis fiber inject — the loader passes all module exports as an object plugin). */
 export const inject = ['slots', 'theme']
 
 /**
  * Client plugin body: provide ctx.layout, then one register() call — AppFrame
- * into 'root' with the four child-slot declarations, the layout store seat,
+ * into 'root' with the five child-slot declarations, the layout store seat,
  * and the inject hook that hands the store's bound actions to the service.
  * @param ctx - client root context.
  */
@@ -123,6 +143,9 @@ export function apply(ctx: ClientContext): void {
         'sidebar': { kind: 'single', scope: 'root' },
         'conversation': { kind: 'single', scope: 'session-maybe' },
         'details': { kind: 'single', scope: 'session' },
+        // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"Register the global navbar action child with the root frame."}
+        'shell.navbar.action': { kind: 'list', scope: 'root' },
+        // @meta-intent:end dsh-right-sidebar
         'shell.overlay': { kind: 'list', scope: 'root' },
       },
       // Exclusive store: the factory itself — the framework instantiates per
diff --git a/packages/client/ui-layout/src/client/stores.ts b/packages/client/ui-layout/src/client/stores.ts
index d2de668a9c..052aa11ac1 100644
--- a/packages/client/ui-layout/src/client/stores.ts
+++ b/packages/client/ui-layout/src/client/stores.ts
@@ -1,6 +1,9 @@
+// @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Separate remembered right-column width from transient visibility."}
 /**
- * The root entry's transient layout store: panel geometry as plain widths in
- * px (0 = closed). Module level exports the factory only — a module-level
+ * The root entry's layout store: panel geometry and transient visibility.
+ * The right-column width preference persists independently from its open
+ * state, so every page load starts closed without forgetting the last drag.
+ * Module level exports the factory only — a module-level
  * handle would pin the store's identity in the module
  * cache (a de-facto singleton surviving plugin reloads). register() receives
  * the factory (exclusive use: the framework instantiates per entry), AppFrame
@@ -14,13 +17,20 @@ import {
 } from './columns.ts'
 
 /**
- * Layout store state: panel width preferences in px (0 = closed), plus the
+ * Layout store state: panel width preferences in px plus explicit right-panel
+ * visibility. `sidebar` retains its historic 0 = closed convention. The
  * narrow-viewport pair — `narrow` mirrors AppFrame's breakpoint reading
  * (viewport < SIDEBAR_AUTO_COLLAPSE) so toggleSidebar can pick semantics, and
  * `narrowExpanded` is the manual override that re-expands the auto-collapsed
  * sidebar over the squeezed center without rewriting the width preference.
  */
-type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowExpanded: boolean }
+type LayoutState = {
+  sidebar: number
+  detailsWidth: number
+  detailsOpen: boolean
+  narrow: boolean
+  narrowExpanded: boolean
+}
 
 /**
  * Annotation twin of the actions literal below (the export needs a declared
@@ -34,23 +44,30 @@ type LayoutActions = {
   openDetails: (draft: LayoutState) => void
   closeDetails: (draft: LayoutState) => void
 }
+// @meta-intent:end dsh-right-sidebar
 
 /**
- * Create the layout panel store handle. The preference IS the width, so
- * closing a panel forgets its drag width — reopening restores the contract
- * default. Actions are the complete write set: drag writes clamp
- * into the panel's contract range and never cross the open/closed line;
- * open/close transitions write 0 / the default explicitly. Below the
+ * Create the layout panel store handle. The right-column preference and
+ * visibility are separate: drag writes and persists `detailsWidth`, while
+ * open/close only flips transient `detailsOpen`. Below the
  * auto-collapse breakpoint (AppFrame feeds setNarrow) the sidebar toggle
  * flips the narrowExpanded override instead of the preference.
  * @returns the store handle (spec + type + identity + factory in one).
  */
+// @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"Persist only the remembered width and keep open/close state fresh on every load."}
 export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutActions>  {
   const handle = defineStore({
-    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false }),
+    init: (): LayoutState => ({
+      sidebar: SIDEBAR_DEFAULT,
+      detailsWidth: DETAILS_DEFAULT,
+      detailsOpen: false,
+      narrow: false,
+      narrowExpanded: false,
+    }),
+    persist: { name: 'dsh.layout.panels.v1', keys: ['detailsWidth'] },
     actions: {
       setSidebar: (d, px: number) => { d.sidebar = clampWidth(px, SIDEBAR_MIN, SIDEBAR_MAX) },
-      setDetails: (d, px: number) => { d.details = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
+      setDetails: (d, px: number) => { d.detailsWidth = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
       // Narrow toggles flip only the override: the width preference survives
       // untouched, so re-widening restores the pre-squeeze layout.
       toggleSidebar: (d) => {
@@ -64,9 +81,10 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
         d.narrow = narrow
         d.narrowExpanded = false
       },
-      openDetails: (d) => { if (d.details === 0) d.details = DETAILS_DEFAULT },
-      closeDetails: (d) => { d.details = 0 },
+      openDetails: (d) => { d.detailsOpen = true },
+      closeDetails: (d) => { d.detailsOpen = false },
     },
   })
   return handle
 }
+// @meta-intent:end dsh-right-sidebar
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index d865a5c186..ba60ef5461 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -111,6 +111,9 @@ function drag(handle: Element, fromX: number, toX: number): void {
 }
 
 beforeEach(() => {
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"isolate persisted panel preferences between frame acceptance cases"}
+  localStorage.clear()
+  // @meta-intent:end dsh-right-sidebar
   frameWidth = 1920
   selectedSession.current = 's-test' as SessionId
   selectedSessionBlank.current = false
@@ -121,7 +124,10 @@ beforeEach(() => {
   vi.stubGlobal('cancelAnimationFrame', (h: number) => { clearTimeout(h) })
   window.innerWidth = frameWidth
   Element.prototype.getBoundingClientRect = function () {
-    return { width: frameWidth, height: 1080, top: 0, left: 0, right: frameWidth, bottom: 1080, x: 0, y: 0, toJSON: () => ({}) }
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"model the measured global-navbar seat in frame acceptance tests"}
+    const width = this instanceof HTMLElement && this.hasAttribute('data-shell-navbar') ? 32 : frameWidth
+    // @meta-intent:end dsh-right-sidebar
+    return { width, height: 1080, top: 0, left: 0, right: width, bottom: 1080, x: 0, y: 0, toJSON: () => ({}) }
   }
   // jsdom lacks pointer capture: emulate per-element so hasPointerCapture gates pass.
   const captured = new WeakSet<Element>()
@@ -142,6 +148,63 @@ describe('AppFrame', () => {
     expect(tracks(frame)).toEqual([280, 0])
   })
 
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"verify the global navbar owner contract and measured clearance"}
+  it('supplies the global navbar with live details visibility and authoritative actions', () => {
+    const { frame, slotCalls } = mountFrame()
+    const owner = () => slotCalls.filter(c => c.key === 'shell.navbar.action').at(-1)!.props as {
+      detailsOpen: boolean
+      openDetails(): void
+      closeDetails(): void
+    }
+    expect(frame.querySelector('[data-shell-navbar]')).not.toBeNull()
+    expect(owner().detailsOpen).toBe(false)
+    act(() => { owner().openDetails() })
+    expect(tracks(frame)).toEqual([280, 360])
+    expect(owner().detailsOpen).toBe(true)
+    act(() => { owner().closeDetails() })
+    expect(tracks(frame)).toEqual([280, 0])
+    expect(owner().detailsOpen).toBe(false)
+  })
+
+  it('opens details on a blank session before the first message', () => {
+    selectedSessionBlank.current = true
+    const { frame, slotCalls } = mountFrame()
+    const owner = () => slotCalls.filter(c => c.key === 'shell.navbar.action').at(-1)!.props as {
+      detailsOpen: boolean
+      openDetails(): void
+    }
+
+    act(() => { owner().openDetails() })
+
+    expect(tracks(frame)).toEqual([280, 360])
+    expect(owner().detailsOpen).toBe(true)
+  })
+
+  it('opens details on the new-conversation view without a current session', () => {
+    selectedSession.current = undefined
+    const { frame, slotCalls } = mountFrame()
+    const owner = () => slotCalls.filter(c => c.key === 'shell.navbar.action').at(-1)!.props as {
+      detailsOpen: boolean
+      openDetails(): void
+    }
+
+    act(() => { owner().openDetails() })
+
+    expect(tracks(frame)).toEqual([280, 360])
+    expect(owner().detailsOpen).toBe(true)
+  })
+
+  it('reserves measured navbar room only while details are collapsed', () => {
+    const { frame, instance } = mountFrame()
+    act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
+    expect(frame.style.getPropertyValue('--dsh-shell-navbar-clearance')).toBe('48px')
+    act(() => { instance.actions.openDetails() })
+    expect(frame.style.getPropertyValue('--dsh-shell-navbar-clearance')).toBe('28px')
+    act(() => { instance.actions.closeDetails() })
+    expect(frame.style.getPropertyValue('--dsh-shell-navbar-clearance')).toBe('48px')
+  })
+  // @meta-intent:end dsh-right-sidebar
+
   it('renders the session pair with empty owner shares (sessionId is framework-standard)', () => {
     const { slotCalls, getByTestId } = mountFrame()
     expect(getByTestId('center-content')).toBeTruthy()
@@ -172,7 +235,8 @@ describe('AppFrame', () => {
     expect(slotCalls.map(c => c.key)).toContain('details')
   })
 
-  it('ignores unselected states and closes only when the Session id changes', () => {
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"verify transient details visibility across session transitions"}
+  it('closes when the Session id changes, including a blank Session', () => {
     const { frame, instance, rerenderFrame } = mountFrame()
     expect(tracks(frame)).toEqual([280, 0])
 
@@ -188,12 +252,12 @@ describe('AppFrame', () => {
     selectedSessionBlank.current = true
     act(() => { rerenderFrame() })
     expect(tracks(frame)).toEqual([280, 0])
-    expect(instance.getSnapshot().details).toBe(360)
+    expect(instance.getSnapshot()).toMatchObject({ detailsWidth: 360, detailsOpen: false })
 
     selectedSession.current = 's-next' as SessionId
     selectedSessionBlank.current = false
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 360])
+    expect(tracks(frame)).toEqual([280, 0])
 
     selectedSession.current = undefined
     act(() => { rerenderFrame() })
@@ -207,12 +271,13 @@ describe('AppFrame', () => {
     selectedSession.current = undefined
     const { frame, instance, rerenderFrame } = mountFrame()
     expect(tracks(frame)).toEqual([280, 0])
-    expect(instance.getSnapshot().details).toBe(0)
+    expect(instance.getSnapshot()).toMatchObject({ detailsWidth: 360, detailsOpen: false })
 
     selectedSession.current = 's-first' as SessionId
     act(() => { rerenderFrame() })
     expect(tracks(frame)).toEqual([280, 0])
   })
+  // @meta-intent:end dsh-right-sidebar
 
   it('sidebar slot receives live concession output as owner props', () => {
     const { slotCalls } = mountFrame()
@@ -241,7 +306,9 @@ describe('AppFrame', () => {
     expect(tracks(frame)).toEqual([280, 330])
     const handles = frame.querySelectorAll('[class*="handle"]')
     drag(handles[1]!, 920, 930) // shrink by 10 from the rendered width
-    expect(instance.getSnapshot().details).toBe(320)
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"verify drag updates the width preference independently of visibility"}
+    expect(instance.getSnapshot().detailsWidth).toBe(320)
+    // @meta-intent:end dsh-right-sidebar
   })
 
   it('details column stays mounted at zero width', () => {
diff --git a/packages/client/ui-layout/tests/apply.client.spec.ts b/packages/client/ui-layout/tests/apply.client.spec.ts
index 95f8cad439..1f0e01536f 100644
--- a/packages/client/ui-layout/tests/apply.client.spec.ts
+++ b/packages/client/ui-layout/tests/apply.client.spec.ts
@@ -1,6 +1,8 @@
 // @vitest-environment jsdom
 // Client apply wiring under the terminal register form: ctx.layout provided,
-// ONE register() call declares the three child slots + seats the store factory
+// @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"document the additional shell-owned child slots"}
+// ONE register() call declares the five child slots + seats the store factory
+// @meta-intent:end dsh-right-sidebar
 // + wires the panel actions through the inject hook; teardown cascades
 // (service unprovided + declarations gone + registration cleared). Node half
 // and the invariant companion ride along — one line exposes the aggregate
@@ -40,17 +42,21 @@ describe('ui-layout client apply', () => {
     expect(inject).toEqual(['slots', 'theme'])
   })
 
-  it('provides ctx.layout and registers AppFrame into root with the three child declarations', async () => {
+  it('provides ctx.layout and registers AppFrame into root with the five child declarations', async () => {
     const { ctx, slots } = await bench()
     const fiber = ctx.plugin({ inject: [...inject], apply })
     await fiber.await()
     expect(ctx.get('layout')).toBeInstanceOf(LayoutController)
     // The one register() call occupied 'root'…
     expect(slots.entries('root')).toHaveLength(1)
-    // …and declared the three children in the ledger.
+    // …and declared the five children in the ledger.
     expect(slots.spec('sidebar')).toEqual({ kind: 'single', scope: 'root' })
     expect(slots.spec('conversation')).toEqual({ kind: 'single', scope: 'session-maybe' })
     expect(slots.spec('details')).toEqual({ kind: 'single', scope: 'session' })
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"verify declaration and teardown of the global navbar contract"}
+    expect(slots.spec('shell.navbar.action')).toEqual({ kind: 'list', scope: 'root' })
+    expect(slots.spec('shell.overlay')).toEqual({ kind: 'list', scope: 'root' })
+    // @meta-intent:end dsh-right-sidebar
   })
 
   it('injects no business face and attaches the layout actions', async () => {
@@ -58,7 +64,9 @@ describe('ui-layout client apply', () => {
     const fiber = ctx.plugin({ inject: [...inject], apply })
     await fiber.await()
     const actions = {
-      setSidebar: vi.fn(), setDetails: vi.fn(), toggleSidebar: vi.fn(), openDetails: vi.fn(), closeDetails: vi.fn(),
+      // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"provide the expanded layout action surface to the injection fixture"}
+      setSidebar: vi.fn(), setDetails: vi.fn(), toggleSidebar: vi.fn(), setNarrow: vi.fn(), openDetails: vi.fn(), closeDetails: vi.fn(),
+      // @meta-intent:end dsh-right-sidebar
     }
     const injected = (slots.entries('root')[0]!.inject as (actions: never) => object)(actions as never)
     expect(injected).toEqual({})
@@ -100,6 +108,9 @@ describe('ui-layout client apply', () => {
     expect(ctx.get('layout')).toBeUndefined()
     expect(slots.entries('root')).toHaveLength(0)
     expect(slots.spec('sidebar')).toBeUndefined()
+    // @meta-intent:begin dsh-right-sidebar {"region":"shell.navbar.action","purpose":"verify navbar declaration removal follows plugin lifecycle"}
+    expect(slots.spec('shell.navbar.action')).toBeUndefined()
+    // @meta-intent:end dsh-right-sidebar
     // The built-in root declaration survives entry teardown (runtime-owned).
     expect(slots.spec('root')).toEqual({ kind: 'single', scope: 'root' })
   })
diff --git a/packages/client/ui-layout/tests/layout-store.client.spec.ts b/packages/client/ui-layout/tests/layout-store.client.spec.ts
index c6f0069197..c535f2379e 100644
--- a/packages/client/ui-layout/tests/layout-store.client.spec.ts
+++ b/packages/client/ui-layout/tests/layout-store.client.spec.ts
@@ -1,7 +1,7 @@
 // @vitest-environment jsdom
 /**
  * createLayoutStore unit account: init shape, the action write set (clamp
- * inside actions), and the absence of browser persistence. Uses the
+ * inside actions), and right-width-only browser persistence. Uses the
  * test-sanctioned path: factory self-call + .create() gives the
  * real engine instance (same create path as production).
  */
@@ -12,14 +12,23 @@ import {
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from '@deepseek-ai/dsh-client-ui-layout/src/client/columns.ts'
 
-const PERSIST_KEY = 'dsh.layout.panels'
+// @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"name and isolate the persisted details-width preference"}
+const PERSIST_KEY = 'dsh.layout.panels.v1'
+// @meta-intent:end dsh-right-sidebar
 
 beforeEach(() => { localStorage.clear() })
 
 describe('createLayoutStore', () => {
+  // @meta-intent:begin dsh-right-sidebar {"region":"shell.details.transient-visibility","purpose":"verify width preference is independent from transient details visibility"}
   it('initializes the sidebar at its default width, details closed, wide viewport assumed', () => {
     const { store } = createLayoutStore().create()
-    expect(store.getSnapshot()).toEqual({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false })
+    expect(store.getSnapshot()).toEqual({
+      sidebar: SIDEBAR_DEFAULT,
+      detailsWidth: DETAILS_DEFAULT,
+      detailsOpen: false,
+      narrow: false,
+      narrowExpanded: false,
+    })
   })
 
   it('each create() is an independent instance (factory is not a singleton)', () => {
@@ -36,9 +45,9 @@ describe('createLayoutStore', () => {
     actions.setSidebar(9999)
     expect(store.getSnapshot().sidebar).toBe(SIDEBAR_MAX)
     actions.setDetails(1)
-    expect(store.getSnapshot().details).toBe(DETAILS_MIN)
+    expect(store.getSnapshot().detailsWidth).toBe(DETAILS_MIN)
     actions.setDetails(9999)
-    expect(store.getSnapshot().details).toBe(DETAILS_MAX)
+    expect(store.getSnapshot().detailsWidth).toBe(DETAILS_MAX)
   })
 
   it('toggleSidebar flips closed <-> contract default (drag width forgotten)', () => {
@@ -55,7 +64,13 @@ describe('createLayoutStore', () => {
     actions.setSidebar(400)
     actions.setNarrow(true)
     actions.toggleSidebar()
-    expect(store.getSnapshot()).toEqual({ sidebar: 400, details: 0, narrow: true, narrowExpanded: true })
+    expect(store.getSnapshot()).toEqual({
+      sidebar: 400,
+      detailsWidth: DETAILS_DEFAULT,
+      detailsOpen: false,
+      narrow: true,
+      narrowExpanded: true,
+    })
     actions.toggleSidebar()
     expect(store.getSnapshot().narrowExpanded).toBe(false)
     expect(store.getSnapshot().sidebar).toBe(400)
@@ -74,30 +89,34 @@ describe('createLayoutStore', () => {
     expect(store.getSnapshot().narrowExpanded).toBe(false)
   })
 
-  it('openDetails uses the contract default, preserves an open width, and closeDetails zeroes', () => {
+  it('openDetails and closeDetails do not overwrite the width preference', () => {
     const { store, actions } = createLayoutStore().create()
     actions.openDetails()
-    expect(store.getSnapshot().details).toBe(DETAILS_DEFAULT)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: DETAILS_DEFAULT, detailsOpen: true })
     actions.setDetails(500)
     actions.openDetails()
-    expect(store.getSnapshot().details).toBe(500)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: 500, detailsOpen: true })
     actions.closeDetails()
-    expect(store.getSnapshot().details).toBe(0)
+    expect(store.getSnapshot()).toMatchObject({ detailsWidth: 500, detailsOpen: false })
   })
+  // @meta-intent:end dsh-right-sidebar
 
-  it('does not persist panel geometry', () => {
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"verify only the details-width preference is durable and visibility rehydrates closed"}
+  it('persists only the details width and always rehydrates closed', () => {
     const first = createLayoutStore().create()
     first.actions.setSidebar(400)
     first.actions.openDetails()
     first.actions.setDetails(500)
-    expect(localStorage.getItem(PERSIST_KEY)).toBeNull()
+    expect(JSON.parse(localStorage.getItem(PERSIST_KEY)!)).toEqual({ detailsWidth: 500 })
 
     const second = createLayoutStore().create()
     expect(second.store.getSnapshot()).toEqual({
       sidebar: SIDEBAR_DEFAULT,
-      details: 0,
+      detailsWidth: 500,
+      detailsOpen: false,
       narrow: false,
       narrowExpanded: false,
     })
   })
+  // @meta-intent:end dsh-right-sidebar
 })
diff --git a/packages/client/ui-slots/src/store.ts b/packages/client/ui-slots/src/store.ts
index 64e0f9b4b0..3db5c0dbb4 100644
--- a/packages/client/ui-slots/src/store.ts
+++ b/packages/client/ui-slots/src/store.ts
@@ -1,5 +1,6 @@
 /** Framework-neutral store contracts for slot registrations and the runtime engine. */
 
+// @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Declare the durable subset used by the sidebar layout store."}
 /**
  * Typed selector hook over a snapshot source. Canonical shape for the whole
  * slot system (ui-renderer's engine hook is structurally identical; the
@@ -36,6 +37,20 @@ export type BakedActions<T, A extends ActionsDecl<T>> = {
   [K in keyof A]: A[K] extends (draft: T, ...params: infer P) => void ? (...params: P) => void : never
 }
 
+/**
+ * Persistence declaration for an object store whose durable fields are only
+ * a subset of its live state. Rehydration overlays the stored keys onto a
+ * fresh `init()` value, so transient fields always start from their declared
+ * defaults.
+ */
+export interface StorePersistence<T> {
+  /** localStorage key (scope suffixing follows the string form). */
+  name: string
+  /** Top-level state keys that survive a new store instance. */
+  keys: readonly (keyof T)[]
+}
+// @meta-intent:end dsh-right-sidebar
+
 /**
  * Store declaration spec: initial-state factory (a lambda so every instance
  * gets a fresh state), optional persistence key (mechanical, framework-run),
@@ -43,7 +58,9 @@ export type BakedActions<T, A extends ActionsDecl<T>> = {
  */
 export interface StoreSpec<T, A extends ActionsDecl<T>> {
   init: () => T
-  persist?: string
+  // @meta-intent:begin dsh-right-sidebar {"region":"dsh.store.partial-persistence","purpose":"Accept either legacy whole-store persistence or a keyed durable subset."}
+  persist?: string | StorePersistence<T>
+  // @meta-intent:end dsh-right-sidebar
   actions: A
 }
 
